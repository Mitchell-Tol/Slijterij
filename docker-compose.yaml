services:
  api:
    build:
      context: .
      dockerfile: "Dockerfile"
    image: mitchelltol/slijterij:1.0
    ports:
      - "8081:8080"
    env_file:
      - .env
    volumes:
      - .:/app/
    depends_on:
      - db

  db:
    image: mysql:8.0
    ports:
      - "3307:3306"
    volumes:
      - ./db_dump:/docker-entrypoint-initdb.d
      - mysql_volume:/var/lib/mysql
    env_file:
      - .env
    environment:
      MYSQL_ROOT_PASSWORD: "${DBPASS}"
      MYSQL_DATABASE: "${DBNAME}"
    networks:
      - default

networks:
  default:

volumes:
  mysql_volume:
